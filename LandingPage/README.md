# PathFinder Landing Page

This is the official showcase and landing page for **PathFinder**, a platform built for **HackHazards 2026** that turns disconnected career advice and professional posturing into a queryable knowledge graph of verified experiences instead of opinions.

## How to Run

To run the landing page locally:

```bash
# Install dependencies
npm install

# Start the development server
npm run dev
```
